name: deploy prod

on:
  push:
    branches: [ main ]
    paths:
      - "eimovina-be/**"
      - ".github/workflows/backend-prod.yml"

jobs:
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    
    env:
      PROD_AWS_ACCESS_KEY_ID: ${{ secrets.PROD_AWS_ACCESS_KEY_ID }}
      PROD_AWS_SECRET_ACCESS_KEY: ${{ secrets.PROD_AWS_SECRET_ACCESS_KEY }}
      AWS_DEFAULT_REGION: eu-central-1

    steps:
    # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
    - uses: actions/checkout@v2
    - uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - uses: actions/setup-node@v1
      with:
        node-version: '14'

    - name: npm ci
      working-directory: eimovina-be
      run: npm ci

    - name: install AWS CLI
      run: |
        curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
        unzip awscliv2.zip
        sudo ./aws/install --update
        
        rm awscliv2.zip
        rm -r ./aws
    - name: deploy to dev
      working-directory: eimovina-be
      run: |
        CREDS=`aws sts assume-role --role-arn arn:aws:iam::${{ secrets.PROD_ACCOUNT_ID }}:role/prod-ci-role --role-session-name=prod_ci_user`
        export PROD_AWS_ACCESS_KEY_ID=`echo $CREDS | jq -r '.Credentials.AccessKeyId'`
        export PROD_AWS_SECRET_ACCESS_KEY=`echo $CREDS | jq -r '.Credentials.SecretAccessKey'`
        export AWS_SESSION_TOKEN=`echo $CREDS | jq -r '.Credentials.SessionToken'`
        npm run sls -- deploy -s prod