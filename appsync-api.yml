name: ${self:service}-${self:provider.stage}
schema: schema.graphql
authenticationType: API_KEY

apiKeys:
  - name: test
    description: Testing key

mappingTemplates:
  - type: Query
    field: getProperties
    dataSource: fetchProperties
    request: false
    response: false

  # - type: Mutation
  #   field: createNote
  #   dataSource: createNote
  #   request: false
  #   response: false

dataSources:
  - type: AWS_LAMBDA
    name: fetchProperties
    description: Lambda for listing notes
    config:
      functionName: fetchProperties
